import React from "react";
import Layout from "./Layout";

const Projects: React.FC = () => {
    return (
        <div>
            <h1>Projects!</h1>
        </div>
    );
};

export default Projects;